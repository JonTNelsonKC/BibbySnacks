name = "bibby-snacks"
compatibility_date = "2026-07-09"
pages_build_output_dir = "."

[vars]
# Optional shared passcode. Prefer setting it as a Pages secret instead of here.
# Configure ORDER_PIN in the Pages project settings.
#
# Webhook notification:
# WEBHOOK_URL = "https://ntfy.sh/your-hard-to-guess-topic"
# WEBHOOK_KIND = "ntfy" # generic, discord, slack, or ntfy
