name = "bibby-snacks"
compatibility_date = "2026-07-09"
main = "src/worker.js"

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = ["/api/*"]

[vars]
# Optional shared passcode. Prefer setting it as a Pages or Workers secret instead of here.
# Pages: configure ORDER_PIN in the Pages project settings.
# Workers fallback: wrangler secret put ORDER_PIN
#
# Webhook notification:
# WEBHOOK_URL = "https://hooks.example.com/..."
# WEBHOOK_KIND = "discord" # generic, discord, slack, or ntfy
#
# Cloudflare Email Service REST API notification:
# CF_ACCOUNT_ID = "your-account-id"
# ORDER_TO_EMAIL = "you@example.com"
# ORDER_FROM_EMAIL = "snacks@yourdomain.com"
#
# Set CF_EMAIL_API_TOKEN as a Pages or Workers secret.
